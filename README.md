# ybox-tv-ci

Build-only mirror of a private repo — exists so iOS IPAs build on free
public-repo Actions minutes. No development happens here; pushes come from
`tools/push-ci-mirror.sh` in the source repo and force-overwrite history.
